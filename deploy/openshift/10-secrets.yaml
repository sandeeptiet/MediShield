# TEMPLATE ONLY — DO NOT COMMIT REAL VALUES.
# Apply via: oc create secret generic medishield-secrets --from-env-file=.env -n medishield
apiVersion: v1
kind: Secret
metadata:
  name: medishield-secrets
  namespace: medishield
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "REPLACE"
  GOOGLE_CLIENT_ID: "REPLACE"
  GOOGLE_CLIENT_SECRET: "REPLACE"
  JWT_SECRET: "REPLACE"
  NEXTAUTH_SECRET: "REPLACE"
  LANGSMITH_API_KEY: "REPLACE"
  MYSQL_PASSWORD: "REPLACE"
  MYSQL_ROOT_PASSWORD: "REPLACE"
