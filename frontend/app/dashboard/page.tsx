// Phase 5 will fetch the case list and render a sortable table with status badges.

export default function DashboardPage() {
  return (
    <main className="min-h-screen p-8">
      <h1 className="mb-2 text-3xl font-semibold">Dashboard</h1>
      <p className="text-slate-500">Case list — coming in Phase 5.</p>
    </main>
  );
}
